# Cloud-Native Service Monitor
A DevOps automation tool to monitor the health of cloud endpoints.

## Features
- Checks HTTP Status Codes (200, 404, 500).
- Automatically follows redirects.
- Logs history to `service_health.log`.

## How to Run
1. Run `bash setup.sh` to set permissions.
2. Run `./monitor.sh` to check service health.
